<h2>Home Page!!!</h2>

<%= render "layouts/flash" %>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <% @posts.each do |p| %>

      <li><%= link_to p.title, p.link %> | <%= p.user.email %></li>

    <% end %>
  </div>
</div>